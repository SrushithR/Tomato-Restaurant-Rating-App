handler: ratings.handler
name: ratings
package:
  include:
    - ratings.py
    - decimal_encoder.py
  # exclude:
  #   - requirements.txt
events:
  - http:
      path: /rating_app
      method: post
      authorizer:
        type: COGNITO_USER_POOLS
        authorizerId:
          Ref: ApiGatewayAuthorizer
      cors: true